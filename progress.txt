# Codebase Patterns

- **CSS:** All design tokens in :root variables (--color-*, --spacing-*, --radius-*, --transition-*)
- **JS:** IIFE pattern, strict mode, state object for form data
- **HTML:** Semantic sections with .screen class, data-screen attribute, aria-label
- **Naming:** BEM-style CSS (block__element--modifier)
- **Screens:** Each screen is a <section class="screen"> with data-screen="N"
- **Navigation:** goToScreen(index) handles transitions + progress bar updates
- **Progress:** Calculated as (currentScreen / TOTAL_STEPS) * 100

---

## 2026-03-10 01:11 - US-001
- Implemented: Project scaffold with index.html, styles.css, app.js. Dark premium theme (#0a0a0a bg, #c9a96e gold, white text). Google Fonts Inter. Welcome screen with badge, headline, subtext, CTA button. Fixed progress bar at top (0% on welcome). Mobile-first responsive design with breakpoints at 374px, 430px, 768px. Screen transition system ready for multi-step flow.
- Files: index.html, styles.css, app.js
- Learnings:
  - Used 100dvh instead of 100vh for better mobile viewport handling
  - Progress bar uses will-change:width for GPU-accelerated transitions
  - Screen transitions use transform+opacity for 60fps performance (will-change hints added)
  - Included a placeholder screen-1 so CTA navigation can be verified immediately

---

## 2026-03-10 01:14 - US-002
- Implemented: Full multi-step form engine with 9 screens (welcome + 7 questions + thank-you). JS handles forward/back navigation with smooth CSS slide transitions (translateX + opacity, 500ms ease). Choice cards auto-advance after 300ms delay. Back button on all question screens. Keyboard Enter advances through all screen types. Form state stored in JS object and logged as JSON on submit. Photo upload with FileReader preview. GDPR checkbox gates submit button. MutationObserver restores choice selections when navigating back.
- Files: index.html, styles.css, app.js
- Learnings:
  - Choice card auto-advance needs a brief delay (300ms) so users see their selection highlighted before sliding
  - MutationObserver on class attribute changes is a clean way to restore UI state when navigating back to choice screens
  - TOTAL_STEPS changed from 8 to 7 (screens 1-7 are questionnaire, screen 8 is thank-you, screen 0 is welcome)
  - Focus management after screen transitions improves keyboard accessibility (requestAnimationFrame timing)
  - Submit button uses disabled attribute gated by GDPR checkbox change event

---

## 2026-03-10 01:19 - US-003
- Implemented: Added inline SVG icons to 4 personality choice cards (mountain/adventurer, lightbulb/creative, lightning/powerwoman, smiley/relaxed). Enhanced choice card styling with icon+label flex layout, box-shadow on hover, double-border on selection. Added freetext validation for screens 2+3 — Continue button blocked until textarea is non-empty, with animated error messages and red border highlight. Validation clears on input via event delegation.
- Files: index.html, styles.css, app.js
- Learnings:
  - Inline SVGs styled via currentColor inherit color transitions from parent for seamless hover/selection effects
  - Event delegation on formContainer for input events is cleaner than per-field listeners for clearing validation errors
  - max-height + opacity animation is a reliable pattern for revealing/hiding error messages since display:none can't be transitioned
  - Choice cards with icon+label layout use flex with gap for consistent alignment across cards with/without icons

---

## 2026-03-10 01:23 - US-004
- Implemented: Enhanced Screen 4 (lifestyle) with side-by-side visual choice cards (globe icon for adventure, calendar icon for planned) featuring larger icons, hint text, and flex row layout. Enhanced Screen 5 (intent) with inline SVG icons — heart for serious, coffee cup for casual, star for open. Added CSS for visual choice variant (--visual modifier), hint text styling with gold transition on selection, responsive sizing for small phones.
- Files: index.html, styles.css
- Learnings:
  - Side-by-side visual cards (flex-direction: row) work better than a range slider for binary choices — clearer tap targets on mobile, consistent cross-browser
  - choice-card--visual modifier with flex-direction:column + centered text creates a distinct "this or that" visual while reusing all existing JS (auto-advance, selection restore)
  - Hint text below labels uses a separate .choice-card__hint span for additional context without affecting the card's primary label
  - No JS changes needed — existing querySelectorAll('.screen__choices') and data-value patterns handle new card variants automatically

---

## 2026-03-10 01:27 - US-005
- Implemented: Enhanced photo upload screen (screen 6) with premium drag-drop zone featuring camera icon, file type/size hints, and drag-over visual state. Added 5MB file size validation with animated error message. Drag-and-drop support via dragenter/dragover/dragleave/drop events. Mobile camera picker via capture="user" attribute. Photo preview with gold-bordered image and remove button (X icon + red hover). Preview restore on back-navigation via stored data URL in state. Keyboard-accessible upload area with role="button", tabindex="0", and Enter/Space activation.
- Files: index.html, styles.css, app.js
- Learnings:
  - Centralized handlePhotoFile() for both click and drag-drop avoids duplicated validation/preview logic
  - Storing photo data URL on state._photoDataUrl enables preview restore when navigating back to screen 6
  - role="button" + tabindex="0" on a div makes it keyboard-accessible while supporting drag-drop layout
  - upload__error uses same opacity + max-height pattern as input-error for consistent animated reveal
  - Remove button hover turns red (#e74c3c) instead of white for clearer destructive action intent

---

## 2026-03-10 01:31 - US-006
- Implemented: Enhanced contact screen (screen 7) with country code select dropdown (15 countries, defaults to DE +49) + phone number input group. Added @ prefix for Instagram handle input. WhatsApp number is required with validation; Instagram is optional. Submit button validates WhatsApp, combines country code + number into single whatsapp answer, collects all form data as JSON and logs to console. GDPR checkbox continues to gate submit button. Enhanced thank-you screen (screen 8) with inline SVG checkmark icon and WhatsApp hint text. MutationObserver restores contact inputs on back-navigation. Error clearing works for nested input-group layouts.
- Files: index.html, styles.css, app.js
- Learnings:
  - Country code select with flag emojis provides better mobile UX than a text input for international phone numbers
  - Input-group pattern (select + input side-by-side with joined borders) creates a cohesive phone number input
  - @ prefix using input-group__prefix span with border-radius adjustments integrates cleanly with the input field
  - validateContactScreen() is separate from validateCurrentScreen() because submit doesn't go through btn-next flow
  - Error clearing via event delegation needs to check grandparent for nested input-group layouts
  - SELECT elements need to be included in saveCurrentScreenInputs() alongside INPUT and TEXTAREA
